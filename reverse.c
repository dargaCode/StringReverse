#include <stdio.h>
#include <cs50.h>
#include <string.h>

void reverse(char*);

int main(void)
{
    char* input = "Hello, world!";
    printf("%s \n", input);
    int len = strlen(input);
    printf("%i \n", len);
}

void reverse(char* str)
{

}
